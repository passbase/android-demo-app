<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/passbase_blue</item>
        <item name="colorPrimaryDark">@color/passbase_blue</item>
        <item name="colorAccent">@color/passbase_blue</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>

</resources>
